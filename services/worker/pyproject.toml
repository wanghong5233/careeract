[project]
name = "careeract-worker"
version = "0.1.0"
license = "Apache-2.0"
requires-python = ">=3.11,<3.14"
dependencies = [
    "docling",
    "httpx",
    "pydantic-settings",
    "temporalio",
]

[tool.uv]
package = false
